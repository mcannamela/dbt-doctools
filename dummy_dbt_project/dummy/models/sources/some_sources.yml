version: 2

sources:
  - name: sources
    description: Dummy sources
    tables:
      - name: some_old_source
        description: >
          A source that existed in our dbt project and already had documentation and downstream models
        columns:
          - name: old_source_id
            description: Id of the record
            tests:
              - unique
              - not_null
          - name: value
            description: "{{doc('some_old_source__value')}}"
      - name: some_new_source
        description: >
          A new source that has no docs or downstream models
        columns:
          - name: new_source_id
            description: ""
          - name: value
            description: ""
