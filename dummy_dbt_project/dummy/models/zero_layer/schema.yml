
version: 2

models:
  - name: old_zero_layer
    description: "One to one with the old source"
    columns:
      - name: old_id
        description: "Id of the record"
        tests:
          - unique
          - not_null
      - name: old_value
        description: "{{doc('zero_layer__old_value')}}"
      - name: another_value
        description: "It's another value"

  - name: old_one_layer
    description: "A starter dbt model"
    columns:
      - name: old_id
        description: "Id of the record"
        tests:
          - unique
          - not_null
      - name: old_value
        description: "{{doc('one_layer__old_value')}}"
